meta {
  name: 7. Delete Rate Not Found
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/owner/parkings/{{parkingId}}/rates/{{newRateId}}
  body: none
  auth: none
}

script:post-response {
  console.log("response", res.body);
}

tests {
  test("should return 422 for non-existent rate", function() {
    expect(res.status).to.equal(422);
  });

  test("should have error type", function() {
    expect(res.body.type).to.equal("https://httpstatuses.com/422");
  });

  test("should have Unprocessable Entity title", function() {
    expect(res.body.title).to.equal("Unprocessable Entity");
  });

  test("should mention rate not found in detail", function() {
    expect(res.body.detail).to.equal("Rate not found");
  });

  test("should have status 422 in body", function() {
    expect(res.body.status).to.equal(422);
  });
}
